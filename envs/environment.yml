name: preprocess
channels:
  - defaults
dependencies:
  - python<3.11
  - pip
  - numpy
  - matplotlib
  - scipy
  - pandas
  - joblib
  - tqdm
  - pytz
  - notebook
  - pip:
    - --user -r requirements.txt