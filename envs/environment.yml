name: ieeg
channels:
  - defaults
dependencies:
  - python<3.12
  - pip
  - numpy
  - matplotlib
  - scipy
  - scikit-image
  - pandas
  - joblib>=1.3
  - tqdm
  - notebook
  - pip:
    - -r requirements.txt