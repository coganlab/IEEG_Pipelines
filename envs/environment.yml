name: preprocess
channels:
  - defaults
dependencies:
  - python<3.11
  - pip
  - numpy
  - matplotlib
  - scipy
  - scikit-image
  - pandas
  - joblib
  - tqdm
  - pytz
  - notebook
  - pip:
    - -r requirements.txt