name: ieeg
channels:
  - defaults
dependencies:
  - python<3.13
  - pip
  - numpy
  - matplotlib
  - scipy
  - scikit-image
  - pandas
  - tqdm
  - notebook
  - mne::conda-forge
  - pip:
    - -r requirements.txt