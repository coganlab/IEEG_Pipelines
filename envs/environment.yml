name: preprocess
channels:
  - defaults
dependencies:
  - python<3.11
  - pip
  - numpy
  - matplotlib
  - scipy
  - pandas
  - joblib
  - tqdm
  - pytz
  - notebook
  - numba
  - pip:
    - -r requirements.txt